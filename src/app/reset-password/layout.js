export const metadata = {
  title: 'Account Access',
  description: 'Manage access to your PawHome account.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function AccountAccessLayout({ children }) {
  return children;
}
