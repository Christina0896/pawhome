export const metadata = {
  title: 'Verify Phone',
  description: 'Verify the phone number saved to your PawHome profile.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function VerifyPhoneLayout({ children }) {
  return children;
}
