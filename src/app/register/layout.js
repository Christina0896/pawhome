export const metadata = {
  title: 'Create a PawHome Account',
  description: 'Register for PawHome to save listings and post pet ads.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function RegisterLayout({ children }) {
  return children;
}
