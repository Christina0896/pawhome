export const metadata = {
  title: 'Registration Complete',
  description: 'Your PawHome account registration is complete.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function RegisterSuccessLayout({ children }) {
  return children;
}
