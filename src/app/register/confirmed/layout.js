export const metadata = {
  title: 'Email Verified',
  description: 'Your PawHome email address has been verified.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function RegisterConfirmedLayout({ children }) {
  return children;
}
