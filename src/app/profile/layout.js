export const metadata = {
  title: 'My PawHome Profile',
  description: 'Manage your PawHome account details and listings.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function ProfileLayout({ children }) {
  return children;
}
