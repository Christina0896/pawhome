export const metadata = {
  title: 'About PawHome',
  description:
    'Learn about PawHome, an Ireland-focused pet marketplace for responsible pet listings and adoption information.',
  alternates: {
    canonical: '/about',
  },
};

export default function AboutLayout({ children }) {
  return children;
}
