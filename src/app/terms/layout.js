export const metadata = {
  title: 'Terms of Use',
  description: 'Read the PawHome website terms of use.',
  robots: {
    index: false,
    follow: true,
  },
};

export default function TermsLayout({ children }) {
  return children;
}
