export const metadata = {
  title: 'Post a Pet Ad',
  description: 'Create a PawHome pet listing for review.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function PostAdLayout({ children }) {
  return children;
}
