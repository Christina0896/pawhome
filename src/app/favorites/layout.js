export const metadata = {
  title: 'Account Listings',
  description: 'View listings saved in your PawHome account.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function AccountListingsLayout({ children }) {
  return children;
}
